cmake_minimum_required(VERSION 3.0)
find_package(OpenGL REQUIRED)
file(GLOB io_src "*.h" "*.cpp")
add_library(io ${io_src})
target_link_libraries(io PUBLIC glad ${PROJECT_SOURCE_DIR}/libs/GLFW/lib/libglfw3.a)